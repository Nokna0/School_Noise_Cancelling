"""
noise_test.py
-------------
numpy 기초 사용법을 탐색하는 실험 스크립트.

노이즈 캔슬링 구현에 앞서 numpy 배열 연산 방식을
직접 확인하기 위한 학습용 코드다.
"""

# 1. numpy를 이용해서 sin함수를 만들어 보자
import numpy

# ── 1차원 배열 생성 ────────────────────────────────────────
a = numpy.array([1, 2, 3, 4, 5])   # 리스트로 numpy 배열 생성

# 불리언 인덱싱 예시: 3보다 큰 원소만 True
# print(a > 3)   # → [False False False  True  True]

# ── 리스트 슬라이싱 ────────────────────────────────────────
b = [1, 2, 3, 4, 5]

# Python 리스트 슬라이싱 예시
# print(b[3:4])  # → [4]

# ── arange: 균일 간격 배열 생성 ────────────────────────────
# numpy.arange(시작, 끝, 간격) — 끝 값은 포함되지 않음
c = numpy.arange(1, 9, 2)          # 1부터 8까지 2 간격 → [1, 3, 5, 7]

print(c)
