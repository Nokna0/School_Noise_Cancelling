# =============================================================================
# School_Noise_Cancelling / AudioSep 실시간 데모 의존성
# 검증 환경: Python 3.11, Windows 11 (2026-06 기준 실제 설치/동작 확인)
# 설치:  pip install -r requirements.txt
#   (단, torch/torchaudio/torchvision 은 아래 안내대로 별도 설치 권장)
# =============================================================================

# --- 이 프로젝트 자체 의존성 -------------------------------------------------
websockets>=12.0          # 검증: 16.0
numpy>=1.24               # 검증: 2.3.5
scipy>=1.10               # 검증: 1.17.1
soundfile>=0.12           # 검증: 0.13.1

# --- PyTorch (AudioSep 추론용) ----------------------------------------------
# 아래 torch 계열 3종은 반드시 같은 빌드(인덱스)에서 함께 설치하세요.
# CUDA(GPU) 사용 시 (예: CUDA 12.1):
#   pip install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cu121
# CPU 만 사용 시:
#   pip install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0                # 검증: 2.12.0+cpu
torchaudio>=2.0           # 검증: 2.11.0
torchvision>=0.15         # 검증: 0.27.0  (CLAP 인코더가 사용)

# --- AudioSep 본체 의존성 ----------------------------------------------------
# 원본 레포(https://github.com/Audio-AGI/AudioSep)의 environment.yml 핀 버전은
# 2023년 기준이라 최신 torch/numpy 와 충돌합니다. 아래는 최신 호환 버전 기준입니다.
# (checkpoint 다운로드는 README 참고. 첫 실행 시 roberta-base 등을 HF Hub 에서 받음)
pyyaml>=6.0               # 검증: 6.0.3   (구 'No module named yaml' 의 원인)
librosa>=0.10             # 검증: 0.11.0
torchlibrosa>=0.1.0       # 검증: 0.1.0
transformers>=4.40        # 검증: 5.9.0
lightning>=2.2            # 검증: 2.6.5
huggingface_hub>=0.20     # 검증: 1.17.0
ftfy>=6.0                 # 검증: 6.3.1
regex>=2023.0             # 검증: 2026.5.9
timm>=0.9                 # 검증: 1.0.27  (CLAP HTSAT 백본)
h5py>=3.8                 # 검증: 3.16.0
pyloudnorm>=0.1           # 검증: 0.2.0
pandas>=1.5               # 검증: 3.0.3
tqdm>=4.64               # 검증: 4.67.3
braceexpand>=0.1.7        # 검증: 0.1.7
webdataset>=0.2           # 검증: 1.0.2
wget>=3.2                 # 검증: 3.2
